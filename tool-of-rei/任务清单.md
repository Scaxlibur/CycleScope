# 任务清单

## Now
- [ ] 暂无进行中的开发任务；保持 `main` 工作树干净，等待后续发布或答辩需求。

## Next
- [ ] 如需建立新的 release tag，先由用户确认发布口径；不得移动既有 `v1.0.0`、`v1.1.0` 或 `fpga-v1.0.0`。
- [ ] 若要把 F0 两位小数镜像升级为正式发布身份，在干净提交上补同等范围物理审计；否则发布说明必须继续绑定旧 BIN 的20例结论。
- [ ] 若答辩需要，人工确认 `500,000.00Hz` 无裁切、TIME/FFT/1P/3P 到真实 panel flush 的2秒边界，以及单路5 V/屏幕/BNC实物条件；这些不是本轮自动证据。

## Backlog
- [ ] 明确 AD9226 MODE/DFS、模拟极性与 OTR 的完整原理图/实测链。
- [ ] 为 lwIP `close()` 返回失败补独立所有权注入；不能盲目重试可能已释放且被复用的 fd。
- [ ] 若继续扩展产品范围，再系统覆盖高阶任意谐波、相位组合、动态帧长或16384；当前均不属于已发布 Profile。

## Done
- [x] `codex/FPGA@038e981` 已合并到 `main`，merge commit 为 `124b783`；保留 FPGA 基线 `fpga-v1.0.0@038e981`，未切换或修改 FPGA 工作树。四件套历史归档于 `fpga-history/`，M11源码归档于 `test/fpga-m11/`，原始证据/回放数据保留在忽略归档区。
- [x] 合并前最终复核：`git diff --cached --check`、F0 版本化摘要 SHA256、PS CSLP 主机测试及 81 项 Python 主机测试均通过；确认暂存区不含 pcap、镜像、运行日志或原始证据。M11 导入目录经 WaveBench venv 验证为历史归档而非当前独立套件，缺失历史计划/私有配置时按设计 fail-closed，边界已写入迁移说明与已知问题。
- [x] 合并前资料收口：新增根/public/docs/tool-of-rei 索引、测试夹具分类、截图与回放说明、轻量证据摘要和 F0 显示版发布记录；所有摘要逐字节回比原始归档，F0 摘要 SHA/JSON、Markdown 链接和 `git diff --check` 通过。五个 C++ 主机回归与三个 Python self-test 通过；final_calibration unittest 因当前环境缺少 numpy 停在导入阶段，未触碰硬件。
- [x] 仅将 `MEASUREMENTS` 卡片 F0 显示由整数 Hz 提升为 `0.01 Hz`，保留千位分隔；FFT结果与频谱图例整数Hz格式不变。格式边界、66例G参数和ESP-IDF 6.0.2 fresh正式构建PASS，size `0x115170`；正式 `.2` 三段烧录哈希校验、启动、session、首帧LIVE及100帧PASS，尚未提交
- [x] 用户实测最大不失真输入边界约 `380 mVpp`；正式普通输入仍冻结为 `≤250 mVpp`，1 MHz/200 mVpp干扰案例实际总包络 `265.080892 mVpp`，不把380 mVpp扩写成正式量程
- [x] 修复500 kHz端点精修略越界后跳过补偿：仅在既有半bin容差内钳位补偿查询频率，显示仍保留实测频率；实机 `500000.03 Hz / 50.285 mVpk` PASS，真正带外仍不补偿
- [x] 修复三谱线校准投影长期占满Core 1导致 `IDLE1` task watchdog：成功取得帧后固定2 ms协作阻塞；同一250 mVpp三谱线复测和随后11例完整续跑均无WDT/panic
- [x] M8/M9最终合并审计20/20 PASS；最大F0/Vpp/RMS/线频/线幅误差 `0.08 Hz / 1.983 mV / 0.554 mV / 0.25 Hz / 0.805 mVpk`。审计根 `final-p4-release-audit-20260801_190121+0800`，JSON/根清单SHA `abe9ce8c…1a7d / 9e03b4ef…ae18`
- [x] 最终正式 `.2` P4镜像 fresh build、烧录和app全量读回PASS：size `0x114e10`，ELF SHA `11c3c886…18b27`，BIN/读回SHA `d819641d…7007`；DG恢复OFF、DP800写入0、FPGA改动0
- [x] 用户将正式输入包络收窄到 `≤250 mVpp`，所有压缩点只保留历史证据并从拟合、验证和验收排除；`scope-amendment-v2` 冻结44点活动目录（M2/M3/M4/M6=`4/24/9/7`），保留原根清单 `c1698923…710e` 和M4停止审计 `a0780b83…5311` 不改写
- [x] 用24个M3点和9个非压缩M4点冻结 `fit-v2`：profile `0xC5DCDE41`、response SHA `9d1aace3…1c5f`，保留幅度点最大增益偏差 `0.5102%`；随后7个隔离M6点全部PASS且未重拟合，最大幅度/频率误差仅 `0.321 mV / 234.99 Hz`，同时通过3 mV/500 Hz工程目标和5 mV/1 kHz硬门槛
- [x] 适配现场RTM CH1未接入：M6正式参考保持DG4202 50 Ω设置，只同步采RTM CH2和64帧FPGA数据；旧双通道预条件/零点失败尝试均完整归档且未删除。DG最终恢复 `SIN / 100 kHz / 50 mVpp / 50 Ω / OFF`，DP800无写入、FPGA无修改
- [x] 生成并接入只读 `frequency_response_profile_generated.hpp`，`main.cpp` 显式传入profile；补偿/FFT/66例G参数/格式/新鲜度与14项Python回归PASS，ESP-IDF 6.0.2 fresh build size `0x114d90`。ELF/BIN SHA `b4927305…591b / 5b4ec8a5…6b23`，只烧录P4三段且app全量读回逐字节一致；板上日志确认 `.3→.2` session与 `p4_response=ON profile=C5DCDE41`
- [x] 最终系统标定 M2～M3：只读预检、128帧零输入、20/50/100 mVpp 低幅门禁，以及 12 个训练频率升/降共24点全部完成；WaveBench原始包、FPGA被动镜像、命令日志和SHA已归档至 `tool-of-rei/evidence/final-calibration-20260801_145546+0800/`
- [x] 最终系统标定活动 M4 已完成 10 mVpp 弱线3点和 50/250 mVpp 交叉幅度6点；450 mVpp不再属于正式范围。历史上10 kHz点经用户授权实测后确认压缩，200/500 kHz未执行，三者均不进入fit/holdout/验收
- [x] 完成M3独立草表：`m3-draft-v1`只读取24个M3点，生成response/amplifier/chain三份CSV和SHA；最大升降频差 `0.009190%`，未读取M4或保留点
- [x] 补齐P4本地补偿通用框架：六字段身份错配在FFT前fail-closed；校正谱线驱动RMS/Vpp/频谱及重构1P/3P；动态帧、分析日志和UI状态明确区分P4RAW/P4CAL。12项Python、补偿/FFT/66例G参数扫/波形/格式/新鲜度主机回归及ESP-IDF 6.0.2 fresh build均PASS；尚未生成或烧录最终profile
- [x] 经用户明确授权执行首个 `10 kHz / 450 mVpp` 工程点：CH2基波 `4.0271 Vpp`、WaveBench THD `4.3695%`、FPGA THD中位数 `4.4578%`，端到端增益变化 `-3.1224%`，超过1%硬门槛；夹具立即停止整组，200/500 kHz未运行，M5未创建、M6未读取，DG已恢复正式OFF状态
- [x] 完成M4失败分层归因：M3→450 mVpp的 `Ksrc/Gamp/Kadc/Ke2e` 变化为 `+1.0502%/-3.8430%/-0.2976%/-3.1224%`，确认模拟前级压缩主导；若真实输入端接恢复至 `Ksrc=1`，CH2预计约 `2.0800 Vpp`，仅作为整改后重测目标
- [x] 将 `ACTIVE VIEW` 卡片替换为 FPGA/LAN 连接诊断：启动 `CHECKING`；无 CSLP 会话 `NO FPGA LINK`；在线无新鲜帧 `NO VALID DATA`；分析拒绝计数增长 `DATA REJECTED`；新鲜有效帧 `NORMAL`。主机状态机 15 例、正式 `.2` fresh build、Display-only 编译和三段烧录均 PASS；当前板上 PHY Link Up 但 FPGA 未回应 HELLO，只实测确认离线分支，不冒充另两态已上板
- [x] 完成用户收窄口径的 M12 DG→FPGA/LAN→P4 验收：ARB 路径覆盖 H2～H16 掩码 15 例、同一 400 kHz 跨 H2～H16 15 例、4×4 相位 16 例、正式 `u_a/u_b` 7 例、三对 `u_b/u_J` 6 例，另含 66.7 kHz H1/H3/H5 基准，共 60 份 P4 验收。总审计 PASS：最大 F0/Vpp/RMS/谱线频率/幅值误差为 `0.180 Hz / 3.727 / 0.967 / 0.625 Hz / 1.082 mV`；P4 原始 3P 包络相对同 FFT 模型的最大残差为 `0.210 mVrms / 0.599 mVpeak`。u_J 对基线的最大 Vpp 漂移仅 `0.096 mV`。DG 已恢复 `SIN / 100 kHz / 50 mVpp / 50Ω / OFF`，未操作 DP800。
- [x] 以 `v1.1.0` 冻结 ESP32-P4 正式显示优化：mV/Hz 统一格式、当前可见谱线动态纵轴与 `mV/div`、最多 8 条动态横轴，以及移除右下角 LVGL FPS/CPU 叠层；既有 `v1.0.0` 与 FPGA 专用 `fpga-v1.0.0` 均不移动
- [x] 移除右下角 LVGL 性能调试叠层：正式 defaults 关闭 `LV_USE_PERF_MONITOR`，保留 CycleScope footer 与 sysmon 诊断能力；fresh 配置、ELF 符号和正式构建审计均证明 FPS/CPU label 未进入镜像。新正式 `.2` 三段烧录校验 PASS，session `E1841489` 接收 400 帧且启动门禁正常；当前输入无有效谱线，故只记链路回归、不冒充测量通过
- [x] 频谱纵轴改为 UI 视窗层按当前可见 1～8 条动态缩放：最高线占连续纵轴 80%，五格纵轴显示两位小数 `mV/div`，按 `−/+` 同步重算；分析侧固定档位/迟滞和 FFT 测量值不变。投影 ASan/UBSan、格式边界、66 例 G 参数扫、正式/Display-only 构建均 PASS；板测两轮 `3→8→7` 得到 `11.00→15.00→11.00mV/div`，正式 `.2` 恢复后真实 FPGA session `C0664BFE` 连续至 400 帧且首帧显示 `17.30mV/div`
- [x] 统一 LVGL 测量格式：Vpp/RMS 显示为两位小数 `mV`，F0 和频谱图例前三条显示为四舍五入到 1 Hz、带千位分隔的 `Hz`；实时值继续直接来自 FFT。纯主机格式边界、66 例 G 参数扫、正式/Display-only ESP-IDF 6.0.2 构建和 `git diff --check` PASS；新正式 `.2` 三段镜像已烧录校验，session `2FDAC359` 完成首帧至 200 帧启动回归
- [x] 用户调整信号发生器的谐波生成参数后，在正式 FPGA 实时输入下确认 `−/+` 可从默认 3 条调至 8 条，动态横轴视觉效果正常；本轮产品改动已随当前 `main` 提交冻结
- [x] 频谱显示扩展为 `1..实际检测数`、上限 8 条：默认 3 条，`−/+` 到边界自动禁用，横轴按前 N 条语义谱线加 10% 边距自动缩放，单线至少 20 kHz 观察窗并钳位 0～500 kHz；正式 Vpp/RMS/F0 和“基波+最强两谐波”测量口径不变。主机参数扫/投影 ASan+UBSan、ESP-IDF 6.0.2 fresh build、Display-only 两轮 controls/生命周期矩阵与正式 `.2` 600 帧均 PASS；板上已恢复正式镜像
- [x] 真实 FPGA `.2` 两板链闭环：正式 P4 `.3` 在冷启动约 4.332 s 完成 `HELLO → CONFIG_SET → ENABLE_PUSH`，首帧 144713 产生 H1/H3/H5 measurement 与 UI LIVE，35 秒采集至 600 帧；receiver/pipeline 的 incomplete、duplicate、stale、reject、socket、invalid、FFT failure 全为 0
- [x] 已从截图调试镜像恢复 `dc724d7` 正式 `.2`：fresh normal build 无 local CMake/screenshot 源、snapshot 符号或 TCP `50002` 字符串；三段烧录校验、app 区 `0x1132f0` 全量读回和正式 UART 均 PASS，端口探测明确 `Connection refused`
- [x] LVGL 调试截图通道实机闭环：本地 CMake 片段启用 snapshot API，P4 TCP `50002` 仅接受电脑 `.4` 的 `SHOT` 请求；两次 `1024×600` RGB565 截图均在约 `35.8 ms` 生成，PNG/原始像素/JSON 已留存于 `tool-of-rei/screenshots/`，两帧原始 SHA256 相同
- [x] 新增 `tool-of-rei/test/cslp_real_fpga_pcap_replay.py` 并审计 `source_data_for_test`：51/51 份 real-adc 归档哈希、manifest/LAN/pcap 报告和完整 12 包帧解析均 PASS。精确重放 10 kHz/50 mVpp 单音的 session `01B1B679` 完成 23 帧/276 包，P4 按产品门禁预期全部 `valid=0 lines=1`；由 10/100 kHz 两份真实 ADC 捕获逐样点相加的派生 H1+H10 session `29E84C98` 完成 23 帧并得到 `F0=10000.02 Hz`、`23.557/23.607 mVpk` 两线、UI LIVE→STALE→OFFLINE。派生项不冒充 FPGA 同时双音采集；测试后已恢复正式 `.2` 三段镜像，app 全量读回 SHA256 仍为 `23ae1c37…8119`
- [x] 从 `CycleScope-main/main@94dab8f-dirty` fresh 构建并烧录正式 `.2` 无夹具镜像：peer=`192.168.10.2`、`-O2`、heap trace/diagnostic/DISABLE/fault fixture 全关闭，size `0x1132f0`，ELF/BIN SHA256 `c3d6e685…fd448 / 23ae1c37…8119`；三段写后校验通过，Flash app 区全量读回与 BIN `cmp IDENTICAL`。45 秒冷启动中 P4 自检、IP101、`.3:50001→.2:50000` 全正常，但 42 次 HELLO 均未收到 ACK，故只证明正式 P4 已就绪，不宣称 FPGA 联调成功
- [x] 修复“会话在线但长期无有效新帧时旧数据仍显示 LIVE”：新鲜度只由成功应用到 UI 的 current 有效帧续期，`1000 ms` 后在 250 ms 轮询内锁存 STALE；板上 session `7BACBD99` 以 20 帧后持续 STATUS 证明 `LIVE→ONLINE STALE`，peer-silent 后转 `OFFLINE STALE`，session `7BACC34A` 证明只有新 measurement 才 `STALE→LIVE`。主机 9 个边界、Display 私有契约 fresh build、ESP-IDF 6.0.2 normal build、烧录及 app 区读回均 PASS；最终 size `0x1132f0`，ELF/BIN SHA256 `2ebee5dd…/9762c4ab…`
- [x] normal v5 四边界 final4 串口闭环并固化严格证据：sessions `BA29D2A6`～`BA29D2A9`、4×100 帧/4800 包、累计 completed=400、最终 peer-silent→STALE，最大 F0/Vpp/RMS/线频/线幅误差 `0.060 Hz / 0.004 mV / 0.001 mV / 0.120 Hz / 0.003 mV`；final4/final2 与 canonical-log SHA 正例 PASS，70 个日志/内容负例及 1 个 CLI 冲突全部拒绝
- [x] 修复前无夹具 normal `.5` v5 历史基线已 fresh 构建、烧录并完成 app 区全量读回：size `0x112f20`，ELF/BIN SHA256 `6d15ace8…/075c3ec7…`；从 Flash `0x20000` 读回 `0x112f20` 字节后与 BIN `cmp IDENTICAL`。该镜像承载后续 10k/矩阵证据，但不含最终在线数据新鲜度修复
- [x] normal v5 正式完成电脑 `.5→.3` 的 10,000 帧/120,000 包长压：session `C540EE79`，frame 1～10,000 全部接收、分析和发布，最终 receiver `completed=acquired=10000`，全部接收/重组/配置/socket/stale/invalid/FFT failure 为 0；FFT 最终平均/最大 `17.491/50.210 ms`，internal 最低 `118811 B`、PSRAM 固定 `28040080 B`，恒定输入仅首帧一次 `NEW_STREAM→100 mVpk`
- [x] normal v5 动态迟滞与断链恢复闭环 PASS：session `E26255F1` 前 40 帧在阈值附近交替而不抖档，frame 41 精确 `100→200 UPSHIFT`、frame 61 精确 `200→100 DOWNSHIFT`；断链 `LIVE→STALE`，新 session `E26255F4` 的首个有效帧才 `STALE→LIVE`，16 个关键篡改负例全部拒绝
- [x] normal v5 重跑 6×100 高阶矩阵与 100 帧滤后残余：高阶 sessions `04E91356`～`04E9135B` 最大 F0/Vpp/RMS/线频/线幅误差 `0.070 Hz / 0.040 mV / 0.009 mV / 0.140 Hz / 0.013 mV`；1 MHz、0.316 mVpk H50 残余 session `325A36C4` 只输出 20/60/100 kHz 三线，并在数据源退出后正确进入 STALE。两项只证明电脑合成的 P4 数字链，不冒充真实前端抗干扰
- [x] 当前无夹具 `.5` v4 已 fresh 构建、烧录并读回逐字节一致：size `0x1129a0`，ELF/BIN SHA256 `0c36dc58…/b4793a9f…`，本地测试片段为空、`-O2`、heap trace 与全部 fault/diagnostic consumer 关闭；最坏 `5 mVpk H1 + H49/H50` session `0A8E2417` 完成 600 帧/7200 包，`acquired=analyzed=published=600`、stale/invalid/fft_fail 全零，FFT 最大 `19.116 ms`，UI 动态轴为 `50.0 mVpk`
- [x] 修复高次谐波占优时复合波形首个上升过零会跨帧跳分支的问题：FFT 结果携带 H1 phase，1P/3P 共用靠近 Hann 窗中心的等价 H1 上升零点；普通 H1/H3/H4 与弱 `5 mVpk H1 + H49/H50` 的 phase wrap 扫描在 `-Werror + ASan/UBSan` 下满足跨相位相关性/最大差门禁，NaN/Inf phase fail-closed；板上 normal v4 的普通与 exact-weak phase 启动门禁均 PASS
- [x] Display-only v7 程序化执行 `TIME → FFT → TIME → 1P → 3P`：两轮 `controls=PASS`，最大单次 LVGL 软件回调 `18.793 ms`；D0 strict、D1/D2、D3～D9 继续 journal/tree/heap EXACT、owned EMPTY、retry PASS，无 panic/WDT。该证据只覆盖回调/重绘提交，不冒充真实 panel flush
- [x] 当前 v4 重新在线覆盖四个 G 题边界各 100 帧：session `78EB6CCB/78EB6CD1/78EB6CDE/48CA72FC` 的 frame 1/100 数值一致，最大 F0/Vpp/RMS/谱线频率/幅值误差 `0.060 Hz / 0.004 mV / 0.001 mV / 0.120 Hz / 0.003 mV`，无 measurement/projection/FFT reject、WDT、panic 或 assert
- [x] 当前 v3 的 10,000 帧证据夹具完成最终防伪审计：锁定唯一且整行精确的 sender 完成语义、542 秒 wall time、单调 UART 时间戳、501.325 秒正式流与 40 秒 hold 尾窗，并由 `tool-of-rei/test/cslp_v3_long_stability_adversarial_test.py` 固化 19 个伪造负例全部拒绝；正式日志仍输出 `digital_stability=PASS`
- [x] 当前无夹具 `.5` v3 完成电脑合成源 10,000 帧/120,000 包长压：正式 session `1700DCF3` 的 101 个 frame/measurement 检查点均满足 `frame=completed=gen`，最终 receiver `completed=acquired=10000` 且所有接收、重组、配置和 socket 错误为 0；pipeline 每 600 帧精确闭环，frame 10000 measurement 位于发布计数递增之后，FFT 平均/最大 `16.888/24.733 ms`，内存无不可恢复下降，严格证据夹具输出 `digital_stability=PASS`
- [x] 用户允许串口采集后，当前无夹具 `.5` v3 统一重放四个 G 题边界与 1 MHz/0.316 mVpk 滤后残余各 100 帧；五个匹配 session `D9AA4EF8`～`D9AA4EFC` 的 frame 1/100、`completed=100/200/300/400/500`、全部可见错误链和 `axis=0.50000MHz` 均由夹具判定 PASS，最大 F0/Vpp/RMS/谱线频率/幅值误差仅 `0.060 Hz / 0.004 mV / 0.001 mV / 0.120 Hz / 0.003 mV`
- [x] 新增只读 `cslp_g_v3_sender_evidence.py`，统一固化四边界与 1 MHz/0.316 mVpk 滤后残余；逐字段锁定波形/Profile/250 µs 分片间隔，校验五份日志共 5 session、500 帧/6000 包及 `.5:50000→.3:50001` 隔离；只有发送日志时强制输出 `board_result=UNPROVEN`，避免电脑发送完成冒充 P4 处理完成
- [x] 当前无夹具 `.5` v3 完成 6 例高阶谐波板测：覆盖 H19/H20/H47/H50、10/10.5/100/250 kHz 基频、200/500 kHz 边界及恰好 5.000 mVpk 分量；session `9B75E003`～`008` 全部产生有效 measurement，最大 F0/Vpp/RMS/谱线频率/幅值误差仅 `0.070 Hz / 0.040 mV / 0.009 mV / 0.140 Hz / 0.013 mV`，`axis=0.50000MHz`，无 measurement reject、FFT failure、WDT 或 panic
- [x] 完整 Display/LVGL/BSP/触摸生命周期闭环：启动改为 display/UI/FFT prepare 全成功后才启动 receiver；持久 stack 按背光、touch/display、adapter、raw touch、I²C、DMA2D、LEDC、BSP display 严格逆序销毁。heap trace 将每轮 96 B 锁定为 LVGL 9.4.0 的 92 B `lv_general_mutex`，tracked 兼容清理后正常 `-O2` 的 D0、D3～D9、D1/D2、幂等 destroy 与同对象 retry 全部 `owned=EMPTY / heap=EXACT / integrity=PASS`；随后烧回无夹具正常 `.5` 并完成 session `0x306ADB08` 的 600 帧零错误回归
- [x] 两块 PSRAM RGB565 画布与唯一 `ui_root_` 已改为事务化 create/destroy；D0 warm/strict、D1 waveform、D2 spectrum 在板上按固定逆序 journal、tree/heap 精确恢复和同实例 retry 全部 PASS。夹具局部 31,776 B `InstrumentApp` 导致 10,240 B main 栈溢出的问题已改为静态复用，并加 `-Werror=frame-larger-than=4096` 防回归；随后烧回无夹具正常 `.5` 镜像并完成 600 帧零错误回归
- [x] socket runtime 独立 RT1～RT4 矩阵 PASS：真实 invalid-fd syscall 覆盖 socket/`SO_RCVTIMEO`/bind/active `recvfrom` fatal；旧 S1 `InUse` 不变且失效、pending 清空、旧分片精确拒绝 12 个，S2 与最终 session 各恢复 100 帧；RT4 共 33 次、34 session、fd `36/35`、heap `348579/33181568 B` 精确恢复；随后烧回无夹具正常 `.5` 镜像并完成 600/600/600 零错误回归
- [x] 将频谱显示数据与轴同步收口到 `0..500 kHz`：只聚合 1009 个带内 Bin，640→638 对 peak/RMS 做区间 max-pool，10/20 kHz 线宽区间不相接，500 kHz 边界与半 Bin 容差可见；纯主机 `-Werror + ASan/UBSan`、ESP-IDF 6.0.2 构建及板上 600 帧均 PASS
- [x] 静态 IP 在 `esp_eth_start()` 前同步预配置并保留 link-up 后幂等设置以发布 GOT_IP；R14 首次 `ESP_ERR_NO_MEM` 后 `owned=EMPTY / heap=PASS / free=EXACT`，R14→R12 证明同一对象可重试，Core1 receiver 矩阵扩展为 14/14 PASS
- [x] 收口 receiver 启动回滚：R7 证明为异步 4 B 命令项延迟回收而非线性泄漏；R13 去除静态 netif 的 DHCP client、STOP 前清空 IP、过滤目标 ETH handle，并以 callback 门闩加 `slot_mutex_` join 消除 Core1 测试与 Core0 event-loop 的晚到 CONNECTED/GOT_IP 竞态；普通 `-O2` 上 P1～P10、Core1 R1～R13 全部 `owned=EMPTY / heap=PASS / free=EXACT`
- [x] 将 startup-fault 构建接入完全收拢到 ignored `tool-of-rei/test/startup_fault_test.cmake`；公开 Kconfig 删除私有开关，公开 CMake 只保留默认空且校验存在性的本地片段入口；独立 `/tmp` 测试构建 PASS
- [x] fresh 构建并烧录无测试污染的正常 `.5` 镜像 `0x10e5f0`；sdkconfig、生成头、CMake cache、compile commands、ELF 符号/字符串五重检查通过，session `C6D56E5C` 精确完成 600 帧/7200 包，`completed=acquired=analyzed=published=600`，错误链为零
- [x] 补齐并在线验证 `DISABLE_PUSH → CONFIG_SET → ENABLE_PUSH`：同 session 丢首 ACK 后字节级幂等重试，ACK 前 frame 9 完整接收，ACK 后旧 config frame 10 静默门禁，新 config frame 11～18 恢复；完成/分析/发布精确 `+17`，retry `+1`、reconnect/reject `+0`
- [x] 修复启动失败生命周期：prepare 失败时 UI 不再卡在 `NETWORK STARTING`；UI timer 改为 analysis task 之前分配，task 失败会删除 timer；资源清理同步清除 self-test 状态
- [x] exact 弱基波已加入 P4 启动门禁：raw S16_LE CRC `0x4ECFD324`、两谱线/H1-H2/F0/Vpp/RMS/bin/FFT 时限全部 fail-closed；板上 `13.928 ms` PASS，镜像 `0x10e2a0` 已构建、烧录和写后校验，ANSI 保持不变
- [x] exact 门禁镜像四例 G 题在线矩阵回归完成；前三例 100/100，案例 4 首轮 `150 µs` 微突发仅 97/100 后以 `250 µs` 重跑精确 `+100/+0`；随后 600 帧全链 `completed/acquired/analyzed/published +600`，错误计数零增长
- [x] 测试夹具源码统一归入 `tool-of-rei/test/`；G 题矩阵 Python 4 例自检及波形投影 C++ `-Werror + ASan/UBSan` 迁移后复测 PASS，`/tmp` 只留二进制、日志和 pcap
- [x] 修复握手完成前 WAVE_DATA 可进入重组链的门禁漏洞：`handle_wave()` 在任何解码/统计前要求 active session；电脑 `.5` 延迟 ENABLE_ACK `61.259 ms` 并精确注入 8 帧/96 包，session-ready 时 `packets=99`（3 ACK+96 WAVE）且全部 frame/reject 计数为 0；ACK 后 frame 9 才使接收/分析/发布/UI 各 `+1`，最终零错误，PASS
- [x] 门禁镜像已用 ESP-IDF 6.0.2 构建、烧录及写后校验：size `0x10c190`，ELF/BIN SHA256 `1f74290d…/13dae704…`；测试夹具固定放在 `tool-of-rei/test/`
- [x] 最终并行 ANSI 镜像同次 `.5` 预监听冷启动已闭环：receiver/session/frame1/measurement/UI bridge=`3440/3501/3515/3533/3559 ms`，frame100=`8486 ms`，100 帧零 incomplete/reject；这些 POWERON 相对时间是冷启动性能事实，不直接判定 G 题按键后 2 秒结果
- [x] 补齐协议扩展回归：cal=0、TEST_PATTERN=0+OTR/FIFO、三类 ACK-loss、STATUS config/state、六类入口拒绝、13 类 WAVE 头拒绝、ACK 语义拒绝恢复、100 帧无 IPv4 分片 pcap 均按受控增量 PASS
- [x] 最终 `.5 + diagnostics off + startup parallel + ANSI FFT` 镜像完成 30 分钟、36,000 帧/432,000 包稳定性复测：正式 session `0x36029302` 的 receiver `completed=4234→40234`、`incomplete=20→20`，pipeline/FFT 全程零 stale/invalid/failure，FFT 平均/最大 `16.740/24.807 ms`，无 WDT/panic/复位或持续内存下降；单帧补取后直接计数 `40235` 闭环确认正式终值
- [x] 4 个固定 G 题边界案例已在最终 ANSI FFT 链上逐例在线重放，每例 frame 1/100 数值一致、完成 100 帧且 incomplete=0；最大 F0/Vpp/RMS/谱线幅值误差约 0.06 Hz/0.004/0.001/0.003 mV，4/4 PASS
- [x] 定位弱基波失败根因为 ESP-DSP 的 P4 ARP4 FFT 内核；同一 exact 样本切换 ANSI 后由 `valid=0 lines=1` 恢复为两谱线有效，最大 FFT 21.175 ms；最终镜像 `0x10c210` 已构建、烧录及回归
- [x] 新增纯离线 G 题矩阵生成器：固定 `.5 → .3` 的 4 个边界案例，覆盖 50/100/250 mVpp、10/200/500 kHz 频率成分及 1/2 个谐波；4 例自检、模拟器自检和 `git diff --check` PASS
- [x] 电脑 `.5` 预监听冷启动基线完成：session/首帧 completed/首个 measurement/首次 UI bridge 为 3477/3491/4027/4263 ms；100 帧、1200 包全部发送完成
- [x] 网络 bootstrap 与 UI/FFT 两阶段启动已上板：UI shell/FFT prepared 提前到 1498/1743 ms，receiver/analysis/UI pipeline 为 3440/3458/3467 ms；无 WDT/panic/复位
- [x] 最终 `.5` 联调镜像含 UI session/frame 日志、FFT prepare 失败显式清理与可重复 Core0/静态分配 defaults；构建、烧录三段哈希校验 PASS
- [x] 冷启动分段诊断完成：POWERON→`app_main` 1191 ms、Ethernet/receiver ready 3441/3462 ms、UI started 3979 ms；`esp_eth_start()` 独占 2205.367 ms；该结果只表征 PHY 冷启动，不直接判定 G 题按键后 2 秒结果
- [x] 电脑 `.5` 正式链 10,000 帧长压完成：120,000 包，接收/分析/发布均净增 10,000，错误计数零增长，无 WDT/panic/复位或持续内存下降
- [x] 修复投影自测失败分支误返回 true；新增 640→638 列聚合保峰断言，10k/40.75k/125k/500kHz 在 `-Werror + ASan/UBSan` 下真实 PASS
- [x] 最终 `.5 + diagnostics off` 镜像已构建并烧录；600 帧/7200 包下 `acquired=analyzed=published=600`，三谱线误差、内存与 WDT 自动回归 PASS
- [x] 正式接线已实现：唯一 Core 1/prio 4 分析消费者按 `acquire → FFT/包络 → frame_is_current → publish → release` 处理 `FrameView`，本地合成源不再进入 UI
- [x] 电脑 `.5` 与 FPGA `.2` 隔离；5 组 G 题矩阵共 500 帧及额外 1 MHz 滤后残余 100 帧，F0/Vpp/RMS 误差远低于 1 kHz/5 mV
- [x] 时域真实帧改为 1P/3P 独立保峰投影和 PSRAM RGB565 canvas；修复首轮逐线绘制造成的 IDLE0 WDT，后续累计 1,400+ 有效帧无 WDT/panic/复位
- [x] 节流后的两轮故障套件精确命中：completed 12、incomplete 6、duplicate/stale/CRC/config/metadata 各 2，坏帧未进入分析
- [x] 主机自检通过：模拟器协议/幂等 PASS；时域投影 10k/40.75k/125k/500kHz 在 `-Werror + ASan/UBSan` 下 PASS；ESP-IDF 6.0.2 镜像构建通过
- [x] FFT+main 合并固件已构建并上板：8192 点自检与 4097→640 保峰 PASS，1200 帧时 `15.507/16.606 ms` 平均/最大，stale/失败 `0/0`，无 WDT、panic、重启或内存下降
- [x] `7eaeb32`：用 `espressif/esp-dsp` 实现 ESP32-P4 8192 点 FFT、Hann 窗、单边 4097 Bin 频谱、三谱线/基频/Vpp/RMS 测量和合成数据生成脚本
- [x] FFT 独立长测 10,200 帧：平均/最大 `15.433/16.934 ms`，stale/失败 `0/0`；历史独立阶段 TIME/FFT、1P/3P 手动切换流畅，但该烟测不构成当前最终 LAN 链的按键到实际面板刷新验收证据
- [x] 合并后主机自检、C++ ASan/UBSan 与 ESP-IDF 6.0.2 全量构建通过；正式镜像 `0x104760`，app 分区余量 87%
- [x] `.5` 在线组合回归通过：600 帧/7200 分片，250 ms 持有下采纳 111 帧，immutable/invalid/release/busy 错误均为 0，UI 无丢帧
- [x] 历史合并阶段曾恢复正式 `.2 + diagnostics off` 固件并完成 360 MHz、32 MB PSRAM、IP101、显示/触摸、双核任务与 4097→640 校验；随后曾按电脑联调要求烧录 `.5`，现已再次由顶部所列正式 `.2` 镜像替换
- [x] `45ad41b`：将 `codex/ESP32P4-LAN` 非快进合入 `main@c911db9`，无文本冲突
- [x] `c911db9`：4097 Bin PSRAM A/B、640 列 peak/RMS、RGB565 画布和 20 Hz 显示管线实机通过
- [x] `388b29f`、`471df1a`、`c9e3ae2`：实现并加固 CSLP UDP 接收器、三缓冲、控制幂等和诊断支持
- [x] LAN 验证通过：100 帧短测、7 类故障、10,000 帧压力、1200 帧所有权及跨 session 重连
- [x] LVGL 9.4 显示、GT911 触摸、时域 min/max 包络、双核任务分工和动态频谱轴均已上板验证
- [x] 冻结 Profile 1：AD9226 A、65 MSPS、16 倍抽取、4.0625 MS/s、8192 点、20 frame/s
