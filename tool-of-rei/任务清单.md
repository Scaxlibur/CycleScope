# 任务清单

## Now
- [ ] 用唯一正式消费者把 CSLP `FrameCursor` mailbox 接入 LiveDataPipeline；验收标准：diagnostics 关闭、同时最多持有一个 `FrameView`，发布前通过 `frame_is_current()`，主机测试与 ESP-IDF 构建通过

## Next
- [ ] 将真实 8192 点 FFT/测量接入现有 4097 Bin PSRAM A/B 与 640 列 peak/RMS 显示接口
- [ ] 与真实 FPGA `.2` 联调，并做两板直连冷启动和 10,000 帧复测
- [ ] 完成 LVGL-M7：首个有效界面不超过 2 秒，目标刷新率连续运行 30 分钟

## Backlog
- [ ] Zynq 侧完成 AXI DMA、低通与 16 倍抽取，输出 4.0625 MS/s、8192 点快照
- [ ] 确认 AD9226 MODE/DFS、模拟极性、OTR、50Ω 输入和抗混叠/抗干扰前端
- [ ] 真机验证 DISABLE_PUSH，增加固定正弦向量和启动失败逐资源故障注入
- [ ] 为 FPGA 离线重连增加退避/日志限流，避免约每秒一轮握手日志

## Done
- [x] 合并后主机自检、C++ ASan/UBSan 与 ESP-IDF 6.0.2 全量构建通过；正式镜像 `0x104760`，app 分区余量 87%
- [x] `.5` 在线组合回归通过：600 帧/7200 分片，250 ms 持有下采纳 111 帧，immutable/invalid/release/busy 错误均为 0，UI 无丢帧
- [x] 板卡已恢复正式 `.2 + diagnostics off` 固件；360 MHz、32 MB PSRAM、IP101、显示/触摸、双核任务与 4097→640 校验通过
- [x] `45ad41b`：将 `codex/ESP32P4-LAN` 非快进合入 `main@c911db9`，无文本冲突
- [x] `c911db9`：4097 Bin PSRAM A/B、640 列 peak/RMS、RGB565 画布和 20 Hz 显示管线实机通过
- [x] `388b29f`、`471df1a`、`c9e3ae2`：实现并加固 CSLP UDP 接收器、三缓冲、控制幂等和诊断支持
- [x] LAN 验证通过：100 帧短测、7 类故障、10,000 帧压力、1200 帧所有权及跨 session 重连
- [x] LVGL 9.4 显示、GT911 触摸、时域 min/max 包络、双核任务分工和动态频谱轴均已上板验证
- [x] 冻结 Profile 1：AD9226 A、65 MSPS、16 倍抽取、4.0625 MS/s、8192 点、20 frame/s
